# JARVIS Configuration

# Your Anthropic API key (required for AI analysis)
ANTHROPIC_API_KEY=your_anthropic_key

# Password to protect the app (recommended)
APP_PASSWORD=your_secure_password

# Whisper model size: tiny, base, small, medium, large-v3
# - tiny/base: Fast, lower accuracy (good for CPU)
# - small/medium: Balanced (needs decent CPU or GPU)
# - large-v3: Best accuracy (needs GPU)
WHISPER_MODEL=base

# Optional: Path to your Obsidian vault for context
OBSIDIAN_VAULT_PATH=
